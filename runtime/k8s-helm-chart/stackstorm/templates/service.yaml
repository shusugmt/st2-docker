apiVersion: v1
kind: Service
metadata:
  name: {{ template "stackstorm.fullname" . }}
  labels:
    app: {{ template "stackstorm.name" . }}
    chart: {{ template "stackstorm.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "stackstorm.name" . }}
    release: {{ .Release.Name }}
